{
  "name": "tile",
  "private": true,
  "workspaces": ["apps/web", "apps/functions"],
  "scripts": {
    "dev:web": "vite --config apps/web/vite.config.ts",
    "build:web": "vite build --config apps/web/vite.config.ts",
    "dev:functions": "firebase emulators:start --only functions,firestore",
    "build:functions": "npm --prefix apps/functions run build",
    "deploy:functions": "firebase deploy --only functions"
  }
}
